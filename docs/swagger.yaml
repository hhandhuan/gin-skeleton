definitions:
  response.Result:
    properties:
      code:
        description: 业务码
        type: integer
      data:
        description: 业务消息体
      msg:
        description: 业务消息
        type: string
    type: object
host: 127.0.0.1:8080
info:
  contact: {}
  description: gin-skeleton 示例项目
  title: gin-skeleton
  version: "1.0"
paths:
  /user/create:
    post:
      consumes:
      - application/json
      description: hello echo
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.Result'
      summary: 用户创建
      tags:
      - 用户管理
  /user/details:
    get:
      consumes:
      - application/json
      description: hello echo
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.Result'
      summary: 用户详情
      tags:
      - 用户管理
swagger: "2.0"
